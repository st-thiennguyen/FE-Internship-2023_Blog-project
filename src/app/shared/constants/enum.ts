export enum StorageKey {
  AUTH = 'auth',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}

export enum TypeUploadImage {
  AVATAR = 'avatar',
  COVER_POST = 'cover-post',
  IMAGE_POST = 'image-post',
}
